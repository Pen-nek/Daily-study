190705



0. 용어
- 카디널리티 : 투플의 개수



1. SQL 기초 복습

1) SQL 구문은 텍스트, 날짜 표기 시 영문 작은 따옴표('') 사용

2) OR 구문
SELECT 속성이름
FROM 테이블이름
WHERE (조건식) OR (조건식)


2. 새로 배운 내용

1) 제약조건
Primary key(기본키) = UNIQUE 속성에 NOT NULL이 추가된 것

2) SQL lite는 경량 DB, 주로 핸드폰에 사용